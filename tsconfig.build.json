{
  "extends": "./tsconfig.json",
  "_comment": "scripts/ fica fora do build: sao ferramentas de operacao rodadas via ts-node, nao codigo de producao. Incluir a pasta tambem moveria o rootDir inferido para a raiz do repo, e a saida viraria dist/src/main.js — quebrando o `start:prod` (node dist/main) e o deploy.",
  "exclude": ["node_modules", "test", "dist", "scripts", "**/*spec.ts"]
}
